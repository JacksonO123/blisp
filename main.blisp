(func new-queue (body
	(return (struct items []))
))

(func enqueue queue item (body
	(set queue items (append (get queue items) item))
	(return queue)
))

(var queue (new-queue))
(print queue)
(set queue (enqueue queue 2))
(print queue)
